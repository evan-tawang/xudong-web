enum TypeEnum {
    STAFF = 1,
    VISITOR = 2,
}

enum OnlineStatusEnum {
    ON_LINE = 1, // 在线
    OFF_LINE = 2, // 离线
    BE_BUSY = 3, // 忙碌
}

export default {
    Type: TypeEnum,
    OnlineStatus: OnlineStatusEnum,
};
